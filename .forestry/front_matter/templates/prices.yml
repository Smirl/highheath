---
label: Prices
hide_body: false
fields:
- type: text
  name: title
  label: title
- type: field_group_list
  name: prices
  label: prices
  fields:
  - type: text
    name: num
    label: Number of Cats
    description: The text which will be in the left column of the table on the prices
      page
    config:
      required: true
  - type: text
    name: price
    label: Price
    description: The text which will be in the right column of the table on the prices
      page
    config:
      required: true
  config:
    labelField: num
    min: 1
pages:
- content/prices.md
